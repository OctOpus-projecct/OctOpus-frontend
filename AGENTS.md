# OctOpus 클라이언트 작업 지침

## 프로젝트와 현재 상태
- 친구들과 즐기는 비상업적 PC 설치형 3D 쿼터뷰 온라인 RPG. 동시 접속 상한은 10명이다.
- 이 저장소는 게임 클라이언트다. 웹 프런트엔드로 가정하거나 웹 프레임워크를 초기화하지 않는다.
- 현재는 문서만 있다. Unity + C#을 설계 기준안으로 하며, 엔진 버전·네트워크 라이브러리·빌드 및 테스트 명령은 아직 미설정이다.

## 작업 시작
1. `git status --short`로 기존 변경을 확인하고 사용자 변경을 보존한다.
2. [프로젝트 안내](docs/project.md)에서 기준 요구사항 위치를 확인한다.
3. [작업 라우팅](docs/agent-routing.md)에서 해당 작업 문서와 요구사항의 관련 절만 읽는다.
4. 통신·공용 규칙 변경이면 백엔드의 `AGENTS.md`와 `docs/architecture.md`도 확인한다.

## 필수 원칙
- 입력·카메라·화면·애니메이션·UI를 담당한다. 채집 성공, 아이템 지급, 거래·저장 결과는 서버가 확정한다.
- 서버 응답을 기다리는 표시와 확정된 결과를 구분한다. 클릭만으로 목재 지급이나 교환 완료를 확정하지 않는다.
- 조작 키는 재설정 가능해야 한다. 벌목 기본 키는 스페이스바, 이동은 마우스 지면 클릭이다. 게임 로직에 키를 직접 하드코딩하지 않는다.
- UI 클릭이 지면 클릭으로 전달되어 이동·벌목 취소를 일으키지 않도록 처리한다.
- 공용 계약·게임 규칙은 백엔드 소유 패키지로 공유하는 방향이다. 타입과 공식을 두 저장소에 복사하지 않는다.
- Unity 장면·프리팹·자산을 만들 때 식별자와 참조를 보존한다. `.meta` 파일은 관련 자산과 함께 관리한다. 생성 캐시·빌드 산출물을 소스에 넣지 않는다.
- 실제 도입된 명령만 실행·문서화한다. 실행하지 않은 엔진·다중 클라이언트 검증을 통과로 보고하지 않는다.
- 이슈·커밋·PR·리뷰 요청은 작업 라우팅의 해당 규칙을 따른다. 최종 병합은 사용자가 결정하며 자동 병합을 설정하지 않는다.
- 한국어로 설명·기획 문서를 작성하고 코드 식별자는 영어를 사용한다. 불필요한 전체 문서 로딩과 범위 밖 수정은 피한다.

## 완료 보고
일반 작업 브랜치는 최신 dev에서 시작하고 PR 대상도 dev로 지정한다. main은 별도 릴리스 요청에서만 갱신한다.

변경 내용, 실제 검증 결과, 미검증 범위, 서버 동반 변경 필요 여부를 보고한다. 규칙 변경 시 백엔드 기준 요구사항을 갱신하고 관련 클라이언트 문서도 맞춘다.
